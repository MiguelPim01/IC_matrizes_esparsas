{
    "version": "2.0.0",
    "tasks": [
        {
            "type": "shell",
            "label": "compilacao",
            "command": "make",
            "options": {
                "cwd": "${fileDirname}"
            }
        }
    ]
}